#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define int long long

void solve(){
    int l = 1, r = 1001, m;
    while(l+1<r){
        m1 = (l+r)/3;
        int m1 = m
        printf("%d %d\n",m,m);
        string s;cin>>s;
        if(s=="<")r = m;
        else l = m;
    }
    cout<< "! " << l <<endl;
    cout<<flush;
}

int32_t main(){
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int t=1;
    cin >> t;
    while(t--)solve(); 
    return 0;
}