#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define int double

void solve(){
    int n, l;cin>>n>>l;
    vector<int> a;
    int prev = 0;
    for(int &i: a)cin>>a;
    sort(a.begin(),a.end());
    for(int i=0;i<n;i++){
        
    }
}

int32_t main(){
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int t=1;
    //cin >> t;
    while(t--)solve(); 
    return 0;
}